run = "bash start.sh"
entrypoint = "main.py"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "cd frontend && npm run build && cd .. && uvicorn main:app --host 0.0.0.0 --port 8080"]
deploymentTarget = "reserved-vm"

[[ports]]
localPort = 8080
externalPort = 80
